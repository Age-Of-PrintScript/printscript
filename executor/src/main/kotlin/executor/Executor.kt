package executor

import interpreter.ExecutionResult
import interpreter.InterpreterImpl
import lexer.Lexer


class Executor {
    private val lexer: Lexer = Lex()
    fun execute(source: String): ExecutionResult{


    }
}